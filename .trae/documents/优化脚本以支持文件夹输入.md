我将优化涉及数据输入的脚本，使其能够处理文件夹输入，并改进滑动窗口处理逻辑。具体修改如下：

1. **修改 step1_1_process_raw.py**
   - 添加对输入路径的检查
   - 如果输入是文件夹，处理所有 .txt 文件
   - 为每个文件生成独立的处理后文件

2. **修改 step1_2_extract_features.py**
   - 添加对输入路径的检查
   - 如果输入是文件夹，处理所有 .csv 文件
   - 为每个文件生成独立的特征文件
   - 保存每个文件的合法丢包值

3. **修改 step1_3_discover_patterns.py**
   - 添加对输入路径的检查
   - 如果输入是文件夹，处理所有特征文件
   - 为每个文件生成独立的行为模式

4. **修改 step2_1_train_model.py**
   - 添加对输入路径的检查
   - 如果输入是文件夹，处理所有行为模式
   - 合并所有文件的训练数据
   - 注意滑动窗口处理，确保窗口不跨文件边界

5. **修改 step2_2_generate_samples.py**
   - 添加对输入路径的检查
   - 如果输入是文件夹，处理所有行为模式
   - 为每个文件生成独立的样本

6. **更新 e2e_pipeline.py**
   - 默认处理文件夹输入
   - 添加对输入路径的检查
   - 根据输入类型（文件/文件夹）执行不同的处理逻辑
   - 当输入是文件夹时，遍历所有文件，逐个处理
   - 确保滑动窗口不跨文件边界

7. **改进滑动窗口处理**
   - 在合并多个文件的数据时，确保滑动窗口不跨文件边界
   - 为每个文件独立生成滑动窗口
   - 避免不同文件之间的窗口重叠
   - 确保每个窗口只包含单个文件的数据

这些修改将使脚本能够灵活处理单个文件或文件夹输入，提高脚本的通用性和易用性，同时确保滑动窗口的正确处理。