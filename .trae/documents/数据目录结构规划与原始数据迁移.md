## 数据目录结构规划与原始数据迁移

### 1. 项目数据目录结构设计
创建以下目录结构来组织数据：
```
NPS/
├── data/
│   ├── raw/                 # 原始数据存放目录
│   ├── processed/           # 处理后的数据目录
│   └── results/             # 生成结果目录
│       ├── features/        # 提取的特征
│       ├── patterns/        # 发现的行为模式
│       ├── simulations/     # 生成的仿真数据
│       └── evaluations/     # 评估结果
```

### 2. 原始数据迁移
- 将 `/Users/xiaotuanzi/PycharmProjects/NetworkSimulation/input/` 中的所有 `.txt` 文件拷贝到 `data/raw/` 目录
- 保留原始文件名，便于追溯

### 3. 数据处理适配
- 修改 `data_processing/data_loader.py` 以支持 HoloWAN Recorder File 格式
- 重点提取 Delay1(ms) 和 Loss1(%) 字段
- 转换为项目所需的标准格式（timestamp, delay, loss_rate）

### 4. 生成文件路径配置
- 修改各模块的输出路径，确保所有生成文件都输出到 `data/results/` 下的对应子目录
- 保持目录结构清晰，便于管理和查找

### 5. 验证与测试
- 测试数据加载功能，确保能正确读取和处理迁移后的原始数据
- 验证生成的文件是否都输出到指定目录

### 执行步骤
1. 创建数据目录结构
2. 拷贝原始数据文件
3. 修改数据加载器以支持新数据格式
4. 更新各模块的输出路径配置
5. 测试数据处理流程

这个方案将确保原始数据得到妥善保存，同时所有生成的文件都有固定的存放位置，便于项目管理和后续使用。